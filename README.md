# demo-reitit-ring-swagger

A small demo for a Clojure application used Reitit

## Usage

Run directly: `lein run`

Run in REPL: `lein repl` then `(start)` 

Then open `http://localhost:3212/` in web browser
